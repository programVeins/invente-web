import { component$ } from "@builder.io/qwik";
export default component$(() => {
  return (
    <div class="absolute blur-[0.04rem] h-screen">
      <img src="vectors/GridBG.svg" class="lg:h-full w-screen" />
    </div>
  );
});
